import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.postgresql.pgconfscanner',
  appName: 'PGConfScanner',
  webDir: 'build'
};

export default config;
